<!--
用法：
1. 必备三节：仓库结构 / 环境与命令 / 评审清单 —— 保留。
2. 其余节不适用就**整节删掉**，不要留占位符，不要为凑齐硬编。
3. 填完删除本注释块。
-->

# <项目名> 项目规范

<一句话定位：这是什么项目、技术栈基线>
<最硬的那条约束，如果有的话>

## 仓库结构

```
<目录>/          <一句话职责>
<目录>/          <一句话职责>
```

## 环境与命令

依赖：<需要预先装的东西>

- **<动作>**：`<命令>`
- **<动作>**：`<命令>`

<负向硬约束，如「禁止直接在宿主机跑 X」>

## 测试

- 测试位置：<路径>
- 跑全量：`<命令>`
- 跑单个：`<命令>`

## 编码规范

- <命名约定>
- <格式化与静态检查工具及命令>
- <语言版本基线>

## 边界

- <跨了就不许做的线>
- <不手动编辑、不提交的目录>

## 提交与 PR

- 格式：`<跟随 git log 的既有风格>`
- 一个提交一件事，格式化与逻辑改动分开提交。
- 不提交 <产物目录>、密钥与本地配置。

## 评审清单

- [ ] `<可执行的验证命令>` 通过
- [ ] <可检查的条件>

**以下由人工验证，AI 不要声称已完成**：<需要人眼或真机的项>

## 参考文档

- <说明>：`<路径>`
- <说明，注明触发条件>：`<路径>`
